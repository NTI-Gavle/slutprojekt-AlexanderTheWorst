services:
  redis:
    image: redis:7-alpine
    container_name: game-redis

    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}

    ports:
      - "${REDIS_PORT}:6379"

    env_file:
      - .env

    volumes:
      - redis_data:/data

volumes:
  redis_data: